{
  "buildCommand": "cd datasense-ui && npm install && npm run build",
  "outputDirectory": "datasense-ui/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "VITE_API_URL": "@vite_api_url"
  }
}
